
export enum EAlertType {
  info=`info`,
  error=`error`,
  warn=`warning`,
  warning=`warning`,
  success=`success`,
}

export enum EAlertPos {
  tl=`top-left`,
  tc=`top-center`,
  tr=`top-right`,
  bl=`bottom-left`,
  bc=`bottom-center`,
  br=`bottom-right`,
}